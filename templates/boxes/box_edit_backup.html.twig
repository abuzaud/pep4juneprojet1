{% extends 'layout.html.twig' %}
{% trans_default_domain 'workflow' %}

{% set active = 'workflow' %}

{% block page_title %}
    {{ box.name }}
{% endblock %}

{% block content %}
    <h3>{{ 'Modification de : ' ~ box.name }}</h3>
    <p><a href="{{ path('box_all') }}">{{ 'Revenir à la liste des boxes' | trans }}</a></p>

    {{ form(form) }}
{% endblock %}

